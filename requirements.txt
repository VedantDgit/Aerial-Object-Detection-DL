streamlit
# Minimal, compatible deps so the app can start in hosted environments.
# Heavy ML packages (tensorflow, ultralytics/torch) are optional and can
# be installed separately if you deploy with a compatible Python runtime.
numpy
pillow
pandas
scikit-learn
